import React from 'react';

export default class ViewBookings extends React.Component {
    
    render(){
      return (
        <div>
          VIEW BOOKINGS
          see the next week by default
          check credential  upon load

        </div>
      )
    }
}
